/*----------------------------------------------------------------------------------
 * desc: 交互相关功能
 * ----------------------------------------------------------------------------------*/

export enum InteractionDelay {
  ONE_SECOND = 1000,
  /** 点击的延迟时间 */
  CLICK = 20,
  /** DOM 加载的延迟时间。只是模拟，并不代表真实 */
  DOM_LOAD = 100,
  /** 导航的延迟时间 */
  NAV = 500,
  /** 模拟请求的延迟时间 */
  MOCK_REQUEST = 300,
}
